import styled from 'styled-components/native';

export const ListStyled = styled.FlatList.attrs(() => ({
  contentContainerStyle: {
    paddingBottom: 120,
    paddingHorizontal: 8,

  },

}))`
  width: 100%;
`;
